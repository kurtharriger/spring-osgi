<?xml version="1.0"?>
<project>
	<parent>
		<artifactId>spring-modules</artifactId>
		<groupId>org.springframework.osgi</groupId>
		<version>2.1-m2-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.springframework.osgi</groupId>
	<artifactId>spring-dao</artifactId>
	<packaging>bundle</packaging>
	<name>Spring dao module (OSGi version)</name>
	<version>${project.parent.version}</version>
	<url>http://www.springframework.org</url>


    <properties>
	   <!-- optional imports-->
	   <spring.osgi.import.pkg>
	    javax.naming;resolution:=optional,
	    javax.transaction;resolution:=optional,
	    org.aopalliance.*;resolution:=optional,
	    org.objectweb.jotm*;resolution:=optional,
	    <!-- oracle tx -->
	    oracle.j2ee.transaction;resolution:=optional,
	    com.evermind.server;resolution:=optional,
	    <!-- weblogic tx -->
	    weblogic.transaction;resolution:=optional,
	    <!-- websphere tx -->
	    com.ibm.ws.Transaction;resolution:=optional,
	    com.ibm.ejs.jts.jta;resolution:=optional,
	    !org.w3c.dom,
	    *
	   </spring.osgi.import.pkg>
    </properties>

</project>