<?xml version="1.0"?>
<project>
	<parent>
		<artifactId>spring-modules</artifactId>
		<groupId>org.springframework.osgi</groupId>
		<version>2.1-m2-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.springframework.osgi</groupId>
	<artifactId>spring-webmvc</artifactId>
	<packaging>bundle</packaging>
	<name>Spring webmvc module (OSGi version)</name>
	<version>${project.parent.version}</version>
	<url>http://www.springframework.org</url>

    <properties>
	   <!-- optional imports-->
	   <spring.osgi.import.pkg>
   com.lowagie.text*;resolution:=optional,
   freemarker*;resolution:=optional,
   jxl*;resolution:=optional,
   net.sf.jasperreports.engine*;resolution:=optional,
   org.apache.commons.attributes*;resolution:=optional,
   org.apache.poi.*;resolution:=optional,
   org.apache.struts.tiles*;resolution:=optional,
   org.apache.velocity*;resolution:=optional,
   * 
	   </spring.osgi.import.pkg>
    </properties>
</project>